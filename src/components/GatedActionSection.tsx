import React from 'react';

/**
 * Gated action demo section.
 * Shows the "Verified Merchant Payment Intent" demo using cloakwork-sdk.
 * Populated in M4 tasks.
 */
export function GatedActionSection() {
  return (
    <div style={{ padding: '1.5rem 0' }}>
      <h2 style={{ color: '#e2e8f0', fontSize: '1.1rem', marginBottom: '1rem' }}>
        Gated Action Demo
      </h2>
      <p style={{ color: '#718096', fontSize: '0.875rem' }}>
        SDK-gated action demo — coming in M4.
      </p>
    </div>
  );
}
