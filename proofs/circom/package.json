{
  "name": "cloakwork-circuits",
  "version": "0.1.0",
  "private": true,
  "description": "Cloakwork ZK circuits — Circom 2.0 + snarkjs",
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.4"
  },
  "scripts": {
    "compile": "circom cloakwork.circom --r1cs --wasm --sym --O2 -o ./build/",
    "setup": "bash ../scripts/setup.sh",
    "prove": "node scripts/prove.js"
  }
}
