[package]
name = "cloakwork-types"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
soroban-sdk = { version = "26.1.0" }

[dev-dependencies]
soroban-sdk = { version = "26.1.0", features = ["testutils"] }

